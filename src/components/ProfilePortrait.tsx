import {useRef} from "react";import {profile} from "../data/media";import {energizeWorld} from "./InteractionManager";
export function ProfilePortrait({src=profile.src}:{src?:string}){const ref=useRef<HTMLDivElement>(null);const move=(e:React.PointerEvent)=>{const r=e.currentTarget.getBoundingClientRect(),x=(e.clientX-r.left)/r.width-.5,y=(e.clientY-r.top)/r.height-.5;ref.current?.style.setProperty("transform",`perspective(900px) rotateY(${x*9}deg) rotateX(${-y*7}deg) translate3d(${x*8}px,${y*8}px,0)`)};return <div className="portrait-wrap" data-interactive onPointerMove={move} onPointerLeave={()=>ref.current?.style.setProperty("transform","")} onPointerEnter={()=>energizeWorld(.8)}><div className="portrait-halo"/><div ref={ref} className="portrait-frame">{src?<img src={src} alt={profile.alt}/>:<div className="portrait-placeholder" role="img" aria-label="Profile placeholder for Vijay Jha"><span>VJ</span><i>Portrait<br/>coming soon</i></div>}<div className="portrait-scan"/><small>{profile.eyebrow}</small></div></div>}
