import { useEffect,useRef } from "react";
export function CustomCursor(){const dot=useRef<HTMLDivElement>(null),ring=useRef<HTMLDivElement>(null),ripples=useRef<HTMLDivElement>(null);useEffect(()=>{if(matchMedia("(pointer: coarse)").matches)return;let x=innerWidth/2,y=innerHeight/2,rx=x,ry=y,raf=0;const move=(e:PointerEvent)=>{x=e.clientX;y=e.clientY;dot.current?.style.setProperty("transform",`translate3d(${x}px,${y}px,0)`)};const tick=()=>{rx+=(x-rx)*.16;ry+=(y-ry)*.16;ring.current?.style.setProperty("transform",`translate3d(${rx}px,${ry}px,0)`);raf=requestAnimationFrame(tick)};const over=(e:Event)=>ring.current?.classList.toggle("is-hover",!!(e.target as HTMLElement).closest("a,button,[data-interactive]"));const down=()=>{ring.current?.classList.add("is-down");if(ripples.current){const r=document.createElement("i");r.style.left=x+"px";r.style.top=y+"px";ripples.current.appendChild(r);setTimeout(()=>r.remove(),650)}setTimeout(()=>ring.current?.classList.remove("is-down"),220)};addEventListener("pointermove",move);addEventListener("mouseover",over);addEventListener("pointerdown",down);tick();return()=>{removeEventListener("pointermove",move);removeEventListener("mouseover",over);removeEventListener("pointerdown",down);cancelAnimationFrame(raf)}},[]);return <><div ref={dot} className="cursor-dot"/><div ref={ring} className="cursor-ring"/><div ref={ripples} className="cursor-ripples"/></>}
