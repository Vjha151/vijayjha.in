import {ArrowDownRight,ArrowRight,Mail,MapPin,Phone} from "lucide-react";import {useEffect,useState} from "react";import {AdminPanel} from "./components/AdminPanel";import {AudioManager,playTone} from "./components/AudioManager";import {CustomCursor} from "./components/CustomCursor";import {Gallery} from "./components/Gallery";import {ProfilePortrait} from "./components/ProfilePortrait";import {Scene} from "./components/Scene";import {energizeWorld} from "./components/InteractionManager";import {experience as defaultExperience,hobbies as defaultHobbies,projects as defaultProjects,services,skills as defaultSkills} from "./data/portfolio";
const pathOf=()=>location.pathname.replace(/\/$/,"")||"/",parse=(s?:string)=>{try{const v=JSON.parse(s||"[]");return Array.isArray(v)&&v.length?v:null}catch{return null}};
function Header({go}:{go:(p:string)=>void}){const[open,setOpen]=useState(false);return <header><a className="brand" href="/" onClick={e=>{e.preventDefault();go("/")}}><span>VJ</span><b>VIJAY JHA</b></a><nav className={open?"open":""}>{[["HOME","/"],["ABOUT","/about"],["BLOG","/blog"],["CONTACT","/contact"]].map(([n,p])=><a key={p} href={p} onClick={e=>{e.preventDefault();setOpen(false);go(p);playTone("click")}}>{n}</a>)}</nav><button className="menu" onClick={()=>setOpen(!open)} aria-label="Toggle navigation"><i/><i/></button></header>}
function Title({index,label,title,copy}:{index:string;label:string;title:string;copy?:string}){return <div className="section-title"><span>{index}</span><div><p className="eyebrow">{label}</p><h2>{title}</h2>{copy&&<p className="section-copy">{copy}</p>}</div></div>}
function Home({go,s}:{go:(p:string)=>void;s:any}){const gal=parse(s.gallery_json)||[];return <><section className="hero"><div className="hero-copy"><p className="overline"><i/> TAX • STARTUPS • INVESTMENT</p><h1>{s.hero_title||<>Hello, I’m<br/><strong>Vijay Jha</strong></>}</h1><p className="hero-intro">{s.hero_subtitle||"Turning financial complexity into clarity, confidence and sustainable growth."}</p><div className="hero-actions"><a className="btn primary" href="/about" onClick={e=>{e.preventDefault();go("/about")}}>Explore my work <ArrowRight/></a><a className="btn ghost" href="/contact" onClick={e=>{e.preventDefault();go("/contact")}}>Contact me <ArrowDownRight/></a></div><p className="hero-role">{s.hero_role||"Tax Consultant • Startup Advisor • Investor"}</p></div><ProfilePortrait src={s.profile_image}/><div className="service-strip glass">{services.map((x,i)=><article key={x.title}><b>0{i+1}</b><div><h3>{x.title}</h3><p>{x.text}</p></div></article>)}</div></section><Gallery items={gal}/></>}
function About({s}:{s:any}){const work=parse(s.work_json)||defaultProjects,skills=parse(s.skills_json)||defaultSkills.map(([name,value])=>({name,value})),exp=parse(s.experience_json)||defaultExperience,hobbies=parse(s.hobbies_json)||defaultHobbies.map(title=>({title}));return <><section className="content-section route-top"><Title index="01" label="ABOUT" title={s.about_title||"Clarity at the intersection of numbers and ambition."}/><div className="about-grid"><p className="big-copy">I help people make <em>better decisions</em> when the stakes are high.</p><div className="body-copy"><p>{s.about_text||"For over a decade, I’ve worked alongside entrepreneurs and businesses to bring calm, structure and intelligent perspective to tax and finance."}</p><div className="stats"><div><strong>10<span>+</span></strong><small>YEARS EXPERIENCE</small></div><div><strong>120<span>+</span></strong><small>CLIENTS ADVISED</small></div><div><strong>18<span>+</span></strong><small>STARTUPS GUIDED</small></div></div></div></div></section><section className="content-section"><Title index="02" label="WORK" title="Measured thinking. Meaningful outcomes."/><div className="project-list">{work.map((p:any,i:number)=><article key={i} onPointerEnter={()=>energizeWorld()}><div className="project-no">{p.no||`0${i+1}`}</div><div><span>{p.tag}</span><h3>{p.title}</h3><p>{p.text}</p></div><strong>{p.result}</strong><button><ArrowDownRight/></button></article>)}</div></section><section className="content-section"><Title index="03" label="SKILLS" title="Depth where it matters."/><div className="skills-grid">{skills.map((x:any,i:number)=>{const name=Array.isArray(x)?x[0]:x.name,value=Array.isArray(x)?x[1]:x.value;return <div key={i}><div><span>{name}</span><b>{value}%</b></div><i><em style={{width:`${value}%`}}/></i></div>})}</div></section><section className="content-section"><Title index="04" label="EXPERIENCE" title="Experience, built deliberately."/><div className="timeline">{exp.map((e:any,i:number)=><article key={i}><span>{e.year}</span><i/><div><h3>{e.role}</h3><b>{e.place}</b><p>{e.text}</p></div></article>)}</div></section><section className="content-section beyond-simple"><Title index="05" label="BEYOND WORK" title="Curiosity keeps me moving."/><div className="hobby-chips">{hobbies.map((h:any,i:number)=><span key={i}>{typeof h==="string"?h:h.title}</span>)}</div></section></>}
function Blog({posts}:{posts:any[]}){return <section className="content-section route-top blog-page"><Title index="JOURNAL" label="NOTES & PERSPECTIVES" title="Thinking in public."/><div className="project-list">{posts.length?posts.map(p=><article key={p.id}><div>{p.image&&<img className="blog-thumb" src={p.image}/>}</div><div><span>{p.category}</span><h3>{p.title}</h3><p>{p.excerpt}</p></div></article>):<div className="empty-journal"><h3>The journal is taking shape.</h3><p>New observations will appear here soon.</p></div>}</div></section>}
function Contact({s}:{s:any}){const socials=parse(s.social_json)||[];return <section className="content-section contact route-top"><p className="eyebrow">START A CONVERSATION</p><h2>{s.contact_title||<>Let’s build something<br/><em>worth growing.</em></>}</h2><p>{s.contact_text||"Have a complex tax question, a company to shape, or an idea worth exploring? I’d like to hear about it."}</p><a className="mail-link" href={`mailto:${s.email||"hello@vijayjha.in"}`}>{s.email||"hello@vijayjha.in"}<ArrowDownRight/></a><div className="contact-meta"><span><MapPin/>{s.location||"New Delhi, India"}</span>{s.phone&&<a href={`tel:${s.phone}`}><Phone/>{s.phone}</a>}<a href={`mailto:${s.email}`}><Mail/>Email</a>{socials.map((x:any)=><a href={x.url} key={x.label} target="_blank" rel="noreferrer">{x.label}</a>)}</div></section>}
function PublicSite(){const[path,setPath]=useState(pathOf),[data,setData]=useState<any>({settings:{},posts:[]});useEffect(()=>{fetch("/api/content").then(r=>r.json()).then(setData).catch(()=>{});const pop=()=>setPath(pathOf());addEventListener("popstate",pop);return()=>removeEventListener("popstate",pop)},[]);const go=(p:string)=>{history.pushState({},"",p);setPath(p);scrollTo(0,0);energizeWorld(1)};return <><Scene/><CustomCursor/><Header go={go}/><AudioManager/><main>{path==="/"?<Home go={go} s={data.settings}/>:path==="/about"?<About s={data.settings}/>:path==="/blog"?<Blog posts={data.posts}/>:<Contact s={data.settings}/>}</main><footer><span>© 2026 Vijay Jha</span><span>Tax Consultant • Startup Advisor • Investor</span><a href="/" onClick={e=>{e.preventDefault();go("/")}}>Home ↑</a></footer></>}
export default function App(){return location.pathname.startsWith("/admin")?<AdminPanel/>:<PublicSite/>}
